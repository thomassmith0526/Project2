<section class="jobBoard">
    <button id="add">Add</button>
    <section class="jobs">
        {{!-- {{#each jobs as |job| }}
            <p>
            <span>{{clientName}}</span>
            <span>{{jobName}}</span>
            <span>{{tasks}}</span>
            <span>{{employees}}</span>
            <span>{{date}}</span>
            </p>
        {{/each}} --}}
        <div>This is a job</div>
    </section>
</section>